<form method="post" action="{{ path('app_nature_visite_delete', {'id': nature_visite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nature_visite.id) }}">
    <button class="btn">Delete</button>
</form>
