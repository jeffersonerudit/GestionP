<form method="post" action="{{ path('app_enveloppe_delete', {'id': enveloppe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enveloppe.id) }}">
    <button class="btn">Delete</button>
</form>
