{% extends 'base.html.twig' %}

{% block title %}Enveloppe{% endblock %}

{% block body %}
    <h1>Enveloppe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ enveloppe.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_enveloppe_index') }}">back to list</a>

    <a href="{{ path('app_enveloppe_edit', {'id': enveloppe.id}) }}">edit</a>

    {{ include('enveloppe/_delete_form.html.twig') }}
{% endblock %}
