{% extends 'template.html.twig' %}

{% block title %}Enveloppe index{% endblock %}

{% block body %}
    <h1>Enveloppe index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for enveloppe in enveloppes %}
            <tr>
                <td>{{ enveloppe.id }}</td>
                <td>
                    <a href="{{ path('app_enveloppe_show', {'id': enveloppe.id}) }}">show</a>
                    <a href="{{ path('app_enveloppe_edit', {'id': enveloppe.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_enveloppe_new') }}">Create new</a>
{% endblock %}
