<form method="post" action="{{ path('app_interet_potentiel_delete', {'id': interet_potentiel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ interet_potentiel.id) }}">
    <button class="btn">Delete</button>
</form>
