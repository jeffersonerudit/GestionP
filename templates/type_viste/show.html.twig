{% extends 'template.html.twig' %}

{% block PageTitle %}
	Gestion de Type des Visiteurs
{% endblock %}


{% block title %}TypeViste{% endblock %}

{% block body %}
    <h5>Détail du type de visteur</h5>

<table class="table table-bordered" id="dataTable" width="100%" cellspacing="0">

        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_viste.id }}</td>
            </tr>
            <tr>
                <th>Type_V</th>
                <td>{{ type_viste.TypeV }}</td>
            </tr>
        </tbody>
    </table>

    <div class="d-flex flex-row">

    <a class="btn btn-outline-info btn-sm" href="{{ path('app_type_viste_index') }}">
        <i class="bi bi-x-squarebi bi-x-square" style="font-size: 1.1rem;"></i>
     </a>

    <a class="btn btn-outline-primary btn-sm" href="{{ path('app_type_viste_edit', {'id': type_viste.id}) }}">
        <i class="bi bi-pencil-square" style="font-size: 1.1rem;"></i>
    </a>

    {{ include('type_viste/_delete_form.html.twig') }}
</div>

{% endblock %}
