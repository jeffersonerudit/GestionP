{% extends 'base.html.twig' %}

{% block title %}StatutRendezVous{% endblock %}

{% block body %}
    <h1>StatutRendezVous</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut_rendez_vou.id }}</td>
            </tr>
            <tr>
                <th>Statut_Rdv</th>
                <td>{{ statut_rendez_vou.StatutRdv }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_statut_rendez_vous_index') }}">back to list</a>

    <a href="{{ path('app_statut_rendez_vous_edit', {'id': statut_rendez_vou.id}) }}">edit</a>

    {{ include('statut_rendez_vous/_delete_form.html.twig') }}
{% endblock %}
